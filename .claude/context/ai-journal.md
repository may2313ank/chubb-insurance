# AI Journal — Prompt Log

This file logs every prompt: **when** it was prompted, **what** was asked, **what the response was**, and whether the user **accepted / rejected / challenged** it.

| # | Date | Prompt (what was asked) | Response (what was done) | Outcome |
|---|------|-------------------------|--------------------------|---------|
| 1 | 2026-06-07 | List the coding convention rules from `.claude/rules/java-code-style.md` that will be followed when generating code. | Listed 7 conventions (naming, file/method size limits, no magic values, no boolean params, cyclomatic complexity < 4 / Strategy pattern, no early returns with exceptions, Optional over null). Noted the rules file was empty and offered to persist it. | Accepted |
| 2 | 2026-06-07 | Write those conventions into `.claude/rules/java-code-style.md`. | Wrote the rules file with sections: Naming, Size Limits, Design Decisions, Null Handling. | Accepted |
| 3 | 2026-06-07 | Add logging rules to `.claude/rules/logging.md` (no System.out.println, use SLF4J, no sensitive data, no string concat, required log context, bulk-op logging). | Wrote the logging rules file with 6 rules across Framework, Sensitive Information, Formatting, Required Context, Bulk Operations. | Accepted |
| 4 | 2026-06-07 | Add testing rules to `.claude/rules/testing.md` (JUnit standards, naming convention, JUnit asserts, unit + integration tests). | Wrote the testing rules file under Framework, Test Naming Conventions, Test Coverage. | Accepted |
| 5 | 2026-06-07 | Update `CLAUDE.md` to say the rules files are always referred to when generating anything, and that requirements.md holds the problem statement. | Wrote `CLAUDE.md` linking the three rules files and pointing to `.claude/context/requirements.md`. | Accepted |
| 6 | 2026-06-07 | Set up `.claude/context/ai-journal.md` as the prompt log (what/when prompted, response, accepted/rejected/challenged). | Created this journal with a structured table and backfilled entries 1–6. | Accepted |
| 7 | 2026-06-07 | Define the tech stack (Spring Boot latest−1, JDK 21, Postgres, Flyway, JUnit latest) in `.claude/context/tech-stack.md` and reference it from `CLAUDE.md`. | Wrote `tech-stack.md` with pinned versions (Java 21, Spring Boot 3.4.x, Maven 3.9.x, PostgreSQL 16.x, Flyway 10.x, JUnit 5.11.x) and added a "Tech Stack — Always Apply" section in `CLAUDE.md`. Flagged the "latest−1" interpretation and Maven-vs-Gradle assumption. | Accepted |
| 8 | 2026-06-07 | Create a Spring Boot project with root package `com.chubb.assessment`; do not create any package unless asked. | Generated Maven project (pom.xml with Boot 3.4.1, web/JPA/validation, Flyway, Postgres, JUnit), `Application.java`, context-load test, `application.yml`, and Maven wrapper. Created only the root package. Verified compilation. | Accepted |
| 9 | 2026-06-07 | Rename `Application` to `ChubAssessmentApplication` (class + test) and move the DB name to an env variable in `application.yml`. | Renamed main class and test, removed old files, parameterized datasource URL with `DB_HOST`/`DB_PORT`/`DB_NAME`. Recompiled OK. Flagged the single-"b" spelling vs. `com.chubb` package. | Accepted |
| 10 | 2026-06-07 | Contract-first OpenAPI 3.0 for `/api/policies`: add required deps, viewable in Swagger, define schemas only to begin with, store spec in resources. | Added `springdoc-openapi-starter-webmvc-ui` 2.7.0 to `pom.xml`; created `src/main/resources/static/openapi/policies-api.yaml` with schemas only (Region, PolicyStatus, Money, PolicySummary, PagedPolicyResponse, ErrorResponse) and empty `paths`; pointed Swagger UI at the static spec in `application.yml`. Verified compile + YAML parse. | Accepted |
| 11 | 2026-06-07 | What is the Maven command to run the Spring Boot app? | Gave `./mvnw spring-boot:run` plus variants; flagged that the app needed PostgreSQL reachable to boot. | Accepted |
| 12 | 2026-06-07 | Remove Postgres dependencies so the server can start; add them back later. | Removed `data-jpa`, Flyway, and `postgresql` from `pom.xml` and the datasource/JPA/Flyway blocks from `application.yml`; kept springdoc `api-docs` enabled so the Swagger UI controller registers. Booted app (~0.7s, no DB) and verified `/swagger-ui.html` → 200 and spec → 200. | Accepted |
| 13 | 2026-06-07 | Generate the required entities in the api layer from the OpenAPI spec. | Created 6 response DTOs in `api/dto/response`: records (MoneyResponse, PolicySummaryResponse, PagedPolicyResponse, ErrorResponse) and enums with `@JsonValue` (RegionResponse, PolicyStatusResponse). No `@Schema` annotations (contract-first). Kept `ErrorResponse.path` non-`Optional` (Optional on wire DTOs is an anti-pattern) and flagged it. Compiled OK. | Accepted |
| 14 | 2026-06-07 | Give the facade for `/api/policies` in the api layer only — then corrected to: controller class outline only, no facade interface. | Initially created a `PolicyApi` interface + `PolicyController`; user challenged. Removed the interface, left a single self-contained `PolicyController` outline (GET mapping, `page`/`size` `@RequestParam` with `@Min`, constants, stub throwing `UnsupportedOperationException`). Compiled OK. | Challenged |
| 15 | 2026-06-07 | Why is `PolicyController` giving duplication issues? | Explained there was no real duplication (46 lines, single class/method, clean `javac`); the errors were stale VS Code Java language-server diagnostics after the full-file overwrite. Suggested Reload Window / Clean Java Language Server Workspace. | Accepted |
| 16 | 2026-06-07 | Remove the comments above each method (look dirty) and add this as a rule. | Added a **Comments** section to `java-code-style.md` (no comments/Javadoc above methods; comment only the non-obvious *why*). Removed the `getPolicies` Javadoc and inline TODO. Compiled OK. | Accepted |
| 17 | 2026-06-07 | Define the `/api/policies` endpoint in the OpenAPI spec. | Replaced empty `paths` with `GET /api/policies` (operationId `getPolicies`, `page`/`size` params, `200` → PagedPolicyResponse, `400` & `503` → ErrorResponse). Validated YAML/refs. Noted no `@RestControllerAdvice` yet, so 400/503 shapes are not produced at runtime. | Accepted |
| 18 | 2026-06-07 | Add the changes to the AI journal. | Appended entries 10–18 covering the OpenAPI contract-first work, Postgres removal, DTOs, controller outline, and the new comments rule. | Accepted |
| 19 | 2026-06-08 | From the Policy Data Schema image: create the JPA entity with annotations, add H2 dependency + DB config in yml, add the request DTO with validation, and add GET-by-id + PATCH endpoints. Asked two scope questions; user chose full vertical slice and JPA enums in the entity package. | Built the full slice end-to-end: `PolicyEntity` (+ `LineOfBusiness`/`PolicyStatus` enums, `@Enumerated(STRING)`, `@GeneratedValue(UUID)`, Hibernate `@CreationTimestamp`/`@UpdateTimestamp`); domain `Policy` + `PolicyPatch`; `PolicyRepository`; `PolicyEntityMapper` (enum↔display-value); `PolicyService` (getById/patch, 404 via `ResourceNotFoundException`); `UpdatePolicyRequest` (Bean Validation, partial-update friendly); `PolicyResponse`; api mappers; `GlobalExceptionHandler` (404/400/503, no stack traces); controller `GET /{id}` + `PATCH /{id}`. Added H2 + `spring-boot-starter-data-jpa` to `pom.xml`; env-driven datasource/JPA/H2-console in `application.yml`. Extended the OpenAPI contract with both operations + `PolicyResponse`/`UpdatePolicyRequest` schemas. Added service unit, controller slice, and `@DataJpaTest` integration tests. All 13 tests green. Flagged: tech-stack still names Postgres/Flyway (H2 used per request); full correlation-id/path logging deferred (common/logging unbuilt). | Accepted |
| 20 | 2026-06-08 | Add four capabilities: `GET /api/v1/policies` (pagination + sorting + filtering by status/LOB/date-range/region + free-text search), `GET /api/v1/policies/{id}`, `PATCH /api/v1/policies/flag` (bulk flag by id array), `GET /api/v1/policies/summary` (counts by status, total premium by LOB, expiring-soon count). Asked two contract questions; user chose a new lightweight summary (replacing the old Active/Lapsed + 5-region enums) and effectiveDate-range filtering. | Versioned API to `/api/v1/policies`. Split the controller by responsibility into `PolicyQueryController` (list/get/summary) and `PolicyCommandController` (flag/patch) sharing `PolicyApiPaths`. Filtering via `JpaSpecificationExecutor` + `PolicySpecificationFactory` from a domain `PolicyFilter`; pagination/sorting via Spring `Pageable` (`@PageableDefault`, max-page-size capped in yml). Aggregation via repository projections (`StatusCount`, `LineOfBusinessPremium`) + `countByExpiryDateBetween`; `PolicyExpiry` domain helper centralizes the 30-day rule (DRY). New domain `PolicyStatistics`/`FlagResult`; DTOs `FlagPoliciesRequest`/`FlagPoliciesResponse`/`PolicyStatisticsResponse`; reshaped `PolicySummaryResponse` to the new schema; extracted shared validation into `PolicyConstraints`. Removed superseded `PolicyController`, `RegionResponse`, `PolicyStatusResponse`. Extended `GlobalExceptionHandler` (bad sort/body/filter → 400). Rewrote the OpenAPI contract for all v1 endpoints. Tests: service unit + two controller slices + extended `@DataJpaTest` (spec filter, search, aggregation, expiry window) — 22 green. Boot smoke-tested: list/summary 200, bad filter 400, unknown id 404, empty flag 400, spec served 200. Kept single `PATCH /{id}` alongside `/flag` (literal route wins). | Pending |
| 21 | 2026-06-09 | Add `target` and the Maven wrapper to `.gitignore`. | Added `target/` and `.mvn/wrapper/` entries to `.gitignore` and untracked the already-committed files in those paths via `git rm --cached` (files kept on disk, staged for removal). | Accepted |
| 22 | 2026-06-09 | Check the vulnerabilities (security review of the branch changes). | Ran a security-focused review of the branch diff. Confirmed the filter/search inputs flow through the JPA Criteria API (parameterized — not SQL-injectable) and that error handling/logging leak no stack traces or PII. Two candidate config findings (default-on H2 console; `ddl-auto: update`) were investigated and dismissed below the confidence threshold (H2 console restricted to loopback by `web-allow-others=false`; `ddl-auto` runs against ephemeral in-memory H2 with no untrusted-input path). No HIGH/MEDIUM findings reported; noted the new read/flag endpoints have no authZ (auth out of scope per requirements). | Accepted |
| 23 | 2026-06-09 | Have `GET /api/v1/policies` take its filter from a request body instead of six `@RequestParam`s. Asked an intent question; user chose "keep GET but accept a body". | Created `PolicyFilterRequest` record (same `@Pattern`/`@DateTimeFormat` validation as the params) in `api/dto/request`; added a `toPolicyFilter(PolicyFilterRequest)` overload to `RequestDtoToDomain`; changed `listPolicies` to `@Valid @RequestBody(required = false) PolicyFilterRequest` + `Pageable`, with an `EMPTY_FILTER` fallback when no body is sent. Confirmed `GlobalExceptionHandler` already covers `MethodArgumentNotValidException` and `HttpMessageNotReadableException`. Flagged GET-with-body as non-standard. User subsequently switched the mapping to `@PostMapping`. OpenAPI spec not yet updated. | Challenged |
| 24 | 2026-06-09 | After switching `listPolicies` to `@PostMapping`, the POST verb wasn't showing in Swagger; cross-check why, then add the changes to the journal. | Diagnosed the contract-first setup: Swagger UI is pointed at the static `/openapi/policies-api.yaml` (not annotation-generated `/v3/api-docs`), so it ignores controller annotations and the YAML still declared `get:`. Updated the spec — `/api/v1/policies` `get:` → `post:`, moved the filter fields into an optional `requestBody` referencing a new `PolicyFilterRequest` schema, kept `page`/`size`/`sort` as query params. Fixed the two failing `PolicyControllerTest` cases (405→ switched `get(...)` to `post(...)`; moved the invalid `status` from a query param into a JSON body) — full suite 22 green. Confirmed both source and `target/classes` copies of the spec now say `post`, that nothing was holding port 8080, and that the lingering GET in Swagger was a stale running server + browser cache (resolved by restart + hard refresh). | Accepted |
| 25 | 2026-06-09 | Fix the Swagger error on `GET /api/v1/policies`: "Request with GET/HEAD method cannot have body" (the endpoint was back on `@GetMapping` but still bound the filter via `@RequestBody`). | Root cause: browsers'/Swagger's `fetch` cannot attach a body to GET. Moved the filter to query parameters — changed `listPolicies` to `@Valid @ModelAttribute PolicyFilterRequest` + `Pageable`, removed the now-dead `EMPTY_FILTER` constant, `Optional` fallback, and unused import. Rewrote the OpenAPI `get:` op to expose `status`/`lineOfBusiness`/`region`/`effectiveFrom`/`effectiveTo`/`q` as query params (alongside `page`/`size`/`sort`) and removed the unused `PolicyFilterRequest` request-body schema. Updated the invalid-filter test to send `?status=Unknown`. `PolicyFilterRequest`/`RequestDtoToDomain` unchanged (annotations work for query binding; invalid value still → `MethodArgumentNotValidException` → 400). 8 controller tests green. | Accepted |
| 26 | 2026-06-09 | Why use `@ModelAttribute`? | Explained: it binds the non-simple `PolicyFilterRequest` from query params (correct for GET), vs `@RequestBody` (the bug — GET has no body) or six separate `@RequestParam`s (loses the cohesive DTO). Noted the annotation is technically optional (Spring defaults non-simple args to `@ModelAttribute`) but kept it explicit for readability/disambiguation, and that it always constructs the object — which is why `EMPTY_FILTER`/null-guard could be removed. | Accepted |
| 27 | 2026-06-09 | `CorrelationIdFilter` is required for tracing — a cross-cutting concern in `common`. | Found `common/logging` empty and no correlation handling, so the logging rule "every statement includes the request/correlation ID" wasn't satisfiable. Created `common/logging/CorrelationIdFilter` (`OncePerRequestFilter`, `@Order(HIGHEST_PRECEDENCE)`, auto-registered `@Component`): reuses an inbound `X-Correlation-Id` header or generates a UUID, seeds the SLF4J **MDC** (`correlationId`) before the chain, echoes it as a response header, and clears MDC in `finally` (pooled-thread leak guard). Wired it into log output via `logging.pattern.correlation` in `application.yml` (uses Boot's built-in correlation slot — no full-pattern rewrite), so `PolicyService`'s existing lines gain the ID without code change. Added `CorrelationIdFilterTest` (generate+propagate, reuse inbound header, MDC cleanup) — 3 green. Noted the rule also lists method+path; offered to add those to the MDC too. | Accepted |
